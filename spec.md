i need to wrtie a userfrendly tool that allows a user to ingest data form a local filestyem or a NAS (NFS/SMB) share into a S3 bucket.

the tools should be python based. have CLI user interface that is easy and understanble to for the user to use.
it needs to allow single files, or multi files or entire share/directory ingest to the s3 bucket.
it needs to use multipart upload.
user needs to be able to configurate the s3 bucket easy etc.

Plan and ask questions and improve functonality where fitting.